# DMF Environment Configuration
# MongoDB Atlas URI and local development settings
# ⚠️  NEVER COMMIT THIS FILE - It's in .gitignore

# DMF Environment
DMF_ENV=local
DMF_REGION=us-east-1

# MongoDB Atlas Credentials
DMF_DB_USER=bighomiecash8346
DMF_DB_PASSWORD=YOUR_REAL_DB_PASSWORD_HERE
DMF_DB_NAME=dmf_music_platform
DMF_DB_HOST=dmf-music-platform.pfqrhc.mongodb.net

# Constructed MongoDB Atlas URI (used by all services)
MONGO_URL="mongodb+srv://${DMF_DB_USER}:${DMF_DB_PASSWORD}@dmf-music-platform.pfqrhc.mongodb.net/${DMF_DB_NAME}?retryWrites=true&w=majority"

# Service Ports
LOVABLE_BACKEND_PORT=4000
DOTNET_BRAIN_PORT=5183
GATEWAY_PORT=5000

# Gateway Security
DMF_API_KEY=your-secret-api-key-CHANGE-IN-PRODUCTION

# Node Environment
NODE_ENV=development
